
<script src="http://connect.facebook.net/en_US/all.js"></script>

<script>
$(document).ready(function(){
  $("#read_more_reviews_btn").click(function(){
    $("this").hide();
  });

  $('a.tweet').click(function(e){
    e.preventDefault();
    var loc = $(this).attr('href');
    var title  = escape($(this).attr('title'));
    window.open('http://twitter.com/share?url=' + loc + '&text=' + title + '&', 'twitterwindow', 'height=450, width=550, top='+($(window).height()/2 - 225) +', left='+$(window).width()/2 +', toolbar=0, location=0, menubar=0, directories=0, scrollbars=0');
  });
});

</script>

<div class="detail-page">
  <%= render "restaurants/part1" %>

  <div class="dotted-line line-part-2"></div>

  <%= render "restaurants/part3" %>
  <br>
  <div class="dotted-line"></div>
  <div class="part4">
    <%= render "add_review_restaurant"%>
  </div>
</div>


<!--  initialize the TN3 when the DOM is ready -->
<script type="text/javascript">
  $(document).ready(function() {
    $(".download-menu-button").click(function(){
      $("#download-menu-modal").modal('toggle');
    });
    //Thumbnailer.config.shaderOpacity = 1;
    var tn1 = $('.mygallery').tn3({
      autoplay: true,
      skinDir:"assets/skins",
      // imageClick:"fullscreen",
      image:{
        maxZoom:1.5,
        crop:true,
        // clickEvent:"dblclick",
        transitions:[{
          type:"blinds"
        },{
          type:"grid"
        },{
          type:"grid",
          duration:460,
          easing:"easeInQuad",
          gridX:1,
          gridY:8,
          // flat, diagonal, circle, random
          sort:"random",
          sortReverse:false,
          diagonalStart:"bl",
          // fade, scale
          method:"fade",
          partDuration:360,
          partEasing:"easeOutSine",
          partDirection:"circle"
          }]
      }
    });
  });
</script>

