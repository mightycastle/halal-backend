<%= form_for(@admin_photo, :validate=>true) do |f| %>
  <% if @admin_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_photo.errors.count, "error") %> prohibited this admin_photo from being saved:</h2>

      <ul>
      <% @admin_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :cover %><br />
    <%= f.check_box :cover %>
  </div>
  <div class="field">
    <%= f.label :image_name %><br />
    <%= f.text_field :image_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
